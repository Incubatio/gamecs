// Generated by CoffeeScript 1.4.0
(function() {

  require(['gamejs', 'surfacearray', 'utils/prng'], function(gamejs, SurfaceArray, Alea) {
    return gamejs.ready(function() {
      var alea, b, dims, display, displayArray, i, j, r, seed, simplex, val, _i, _j, _ref, _ref1;
      dims = [600, 400];
      display = gamejs.Display.setMode(dims);
      displayArray = new SurfaceArray(display);
      seed = new Date();
      alea = new Alea(seed);
      simplex = new gamejs.Simplex(alea);
      for (i = _i = 0, _ref = dims[0]; 0 <= _ref ? _i < _ref : _i > _ref; i = 0 <= _ref ? ++_i : --_i) {
        for (j = _j = 0, _ref1 = dims[1]; 0 <= _ref1 ? _j < _ref1 : _j > _ref1; j = 0 <= _ref1 ? ++_j : --_j) {
          val = simplex.get(i / 50, j / 50) * 255;
          r = val > 0 ? val : 0;
          b = val < 0 ? -val : 0;
          displayArray.set(i, j, [r, 0, b]);
        }
      }
      return SurfaceArray.blitArray(display, displayArray);
    });
  });

}).call(this);
