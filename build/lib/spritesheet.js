// Generated by CoffeeScript 1.4.0
(function() {

  define(function(require) {
    var Rect, SpriteSheet, Surface;
    Surface = require('surface');
    Rect = require('rect');
    return SpriteSheet = (function() {
      var _images;

      _images = [];

      function SpriteSheet() {}

      SpriteSheet.prototype.get = function(id) {
        return _images[id];
      };

      SpriteSheet.prototype.load = function(sheet, size) {
        var height, imgSize, numX, numY, rect, surface, width, x, y, _i, _results;
        width = size[0];
        height = size[1];
        imgSize = new Rect([0, 0], size);
        numX = Math.floor(sheet.rect.width / width);
        numY = Math.floor(sheet.rect.height / height);
        _results = [];
        for (y = _i = 0; 0 <= numY ? _i < numY : _i > numY; y = 0 <= numY ? ++_i : --_i) {
          _results.push((function() {
            var _j, _results1;
            _results1 = [];
            for (x = _j = 0; 0 <= numX ? _j < numX : _j > numX; x = 0 <= numX ? ++_j : --_j) {
              surface = new Surface(size);
              rect = new Rect(x * width, y * height, width, height);
              surface.blit(sheet, imgSize, rect);
              _results1.push(_images.push(surface));
            }
            return _results1;
          })());
        }
        return _results;
      };

      return SpriteSheet;

    })();
  });

}).call(this);
