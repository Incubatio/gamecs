// Generated by CoffeeScript 1.4.0
(function() {
  var Events;

  Events = (function() {
    var _eventables, _off, _on, _trigger;

    function Events() {}

    _eventables = [];

    _on = function(name, cb) {
      if (!this._events[name]) {
        this._events[name] = [];
      }
      return this._events[name].push(cb);
    };

    _off = function(name) {
      return delete this._events[name];
    };

    _trigger = function(name, argv, callback) {
      var cb, _i, _len, _ref, _results;
      if (argv == null) {
        argv = {};
      }
      if (callback == null) {
        callback = null;
      }
      if (this._events[name]) {
        _ref = this._events[name];
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          cb = _ref[_i];
          _results.push(cb({
            name: name,
            ctx: this,
            params: argv
          }));
        }
        return _results;
      }
    };

    Events.eventize = function(obj) {
      _eventables.push(obj);
      obj._events = {};
      obj.on = _on;
      obj.off = _off;
      return obj.trigger = _trigger;
    };

    return Events;

  })();

}).call(this);
